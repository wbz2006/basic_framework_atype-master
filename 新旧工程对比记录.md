# 新旧工程对比记录

对比时间：2026-07-26

对比对象：

- 原项目：`basic_framework-master`
- 新项目：`basic_framework_atype-master`

## 总体结论

新项目不是和原项目完全等效的副本，而是面向 RoboMaster A 板重新生成并适配过的工程。主要变化集中在三处：

- 芯片和板级工程从 STM32F407 系列迁移到 STM32F427 A 板工程。
- 原来的 `BMI088 + IST8310 + ins_task` 惯导方案被替换为 `MPU6500` 方案，并在 `mpu6500` 模块中保留 `INS_Init()`、`INS_Task()`、`attitude_t` 等上层接口。
- Makefile 已按新目录结构更新，当前 `mingw32-make -j4` 可以通过，构建结果为最新状态。

## 工程结构变化

### 顶层工程

原项目主要使用：

- `basic_framework.ioc`
- `startup_stm32f407xx.s`
- `STM32F407IGHx_FLASH.ld`
- `Inc/`
- `Src/`
- `CMakeLists.txt`
- `Makefile.upgrade`

新项目主要使用：

- `basic_framework_ATYPE.ioc`
- `startup_stm32f427xx.s`
- `STM32F427IIHx_FLASH.ld`
- `Core/Inc/`
- `Core/Src/`
- `Makefile`

这说明新工程已经切到 STM32F427 A 板的 CubeMX 工程结构，不能简单当作原工程的文件级等价替换。

### BSP 层

新项目相对原项目删除：

- `bsp/flash`
- `bsp/usb`

新项目保留并继续使用：

- `bsp/adc`
- `bsp/can`
- `bsp/dwt`
- `bsp/gpio`
- `bsp/iic`
- `bsp/log`
- `bsp/pwm`
- `bsp/spi`
- `bsp/usart`

其中 `bsp/usart` 仍然使用 `HAL_UARTEx_ReceiveToIdle_DMA()` 的注册式串口接收框架，遥控器、视觉、裁判系统等模块仍通过 `USARTRegister()` 接入。

## Modules 层变化

### 新项目新增模块

- `modules/mpu6500`
- `modules/key`
- `modules/vofa`

### 新项目删除或替换模块

- 删除原项目 `modules/BMI088`
- 删除原项目 `modules/imu`
- 删除原项目 `modules/ist8310`

也就是说，新项目 modules 层和原项目不等效。惯导部分从原来的 `BMI088 + IST8310 + ins_task` 路线换成了 A 板上的 `MPU6500` 路线。

### 保留模块

以下模块目录在新旧工程中都存在：

- `alarm`
- `algorithm`
- `bluetooth`
- `can_comm`
- `daemon`
- `encoder`
- `master_machine`
- `message_center`
- `motor`
- `oled`
- `referee`
- `remote`
- `standard_cmd`
- `super_cap`
- `TFminiPlus`
- `unicomm`

这些目录名字保留，不代表所有内部文件都完全一致；A 板工程中 Makefile 已按当前实际存在的源文件重新收录。

## IMU 和 INS 适配情况

原项目的惯导接口来自：

- `basic_framework-master/modules/imu/ins_task.h`
- `basic_framework-master/modules/imu/ins_task.c`
- `basic_framework-master/modules/BMI088`
- `basic_framework-master/modules/ist8310`

新项目的惯导接口来自：

- `basic_framework_atype-master/modules/mpu6500/mpu6500.h`
- `basic_framework_atype-master/modules/mpu6500/mpu6500.c`
- `basic_framework_atype-master/modules/mpu6500/mpu6500driver.c`
- `basic_framework_atype-master/modules/mpu6500/imu_temperature.c`

当前 `mpu6500.h` 已提供：

- `attitude_t`
- `INS_Init(void)`
- `INS_Task(void)`

因此，应用层如果只依赖原 `ins_task.h` 提供的姿态输出接口，可以改为直接包含 `mpu6500.h`。当前 `application/gimbal/gimbal.c` 已经这样使用：

- `gimbal_IMU_data = INS_Init();`
- 通过 `gimbal_IMU_data->YawTotalAngle`、`Pitch`、`Gyro[]` 给云台控制提供反馈。

但需要注意：新项目没有兼容原来的底层 BMI088/IST8310 注册接口。也就是说：

- 可以对接 `INS_Init()`、`INS_Task()`、`attitude_t` 这一层。
- 不能直接对接 `BMI088Register()`、`BMI088Acquire()`、`IST8310Init()` 这一层。

## 应用层关键变化

### 遥控器和视觉串口

当前新项目 `application/cmd/robot_cmd.c` 中已经改为：

- `RemoteControlInit(&huart1)`
- `VisionInit(&huart6)`

这和 A 板 DBUS 口匹配：DBUS 应接 `USART1_RX/PB7`，`USART1` 配置为 `100000 / 9B / EVEN`。之前如果把遥控器挂到 `huart3`，调试时会看不到遥控器数据。

视觉现在挂到 `huart6`，需要实际接线也对应 `USART6`，并确认视觉端波特率为 115200。

### 云台应用

当前 `application/gimbal/gimbal.c` 已切换为包含 `mpu6500.h`，并通过 `INS_Init()` 获取姿态数据。

但是文件中仍然会初始化 DJI 电机：

- `yaw_motor = DJIMotorInit(&yaw_config);`
- `pitch_motor = DJIMotorInit(&pitch_config);`
- `loader = DJIMotorInit(&loader_config);`

如果实际没有接 DJI 电机，daemon 仍会注册离线检测，所以会出现 `[dji_motor] Motor lost` 一类日志。这不是因为控制逻辑一定在使用电机，而是因为初始化时已经把电机实例注册进系统。

另外，`GimbalTask()` 中仍然读取：

- `yaw_motor->measure.angle_single_round`

所以如果完全不使用 DJI yaw 电机，不能只注释控制分支，还需要同时处理初始化和反馈数据来源，否则可能空指针或持续离线报警。

## FreeRTOS 任务变化

原项目 `Src/freertos.c` 中主要只有默认任务框架。

新项目 `Core/Src/freertos.c` 已增加：

- `StartMOTORTASK()`：调用 `MotorControlTask()`，约 500Hz。
- `StartDAEMONTASK()`：调用 `DaemonTask()`，约 100Hz。
- `StartROBOTTASK()`：调用 `RobotTask()`，约 200Hz。
- `StartINSTASK()`：调用 `INS_Task()`。

当前需要注意一个小问题：

- `Core/Src/freertos.c:133` 创建 `robottask` 后赋值给了 `defaultTaskHandle`，而不是 `robotTaskHandle`。

这通常不影响任务创建本身，但会导致任务句柄记录错误，后续如果要通过 `robotTaskHandle` 管理任务，需要修正。

另一个频率标注问题：

- `StartINSTASK()` 注释写 200Hz，但实际 `osDelay(1)` 接近 1000Hz。

如果 MPU6500 解算负载或姿态刷新周期有要求，建议统一注释和实际延时。

## Makefile 状态

新项目 Makefile 已按当前目录更新，重点包含：

- `Core/Src/*.c`
- `bsp/adc`
- `modules/mpu6500`
- `modules/motor/DMmotor`
- `modules/motor/power_control.c`
- `modules/referee`
- `modules/standard_cmd`
- `modules/super_cap`
- `modules/unicomm`
- `modules/key`
- `modules/vofa`
- SEGGER RTT 源文件和 include 路径

已经移除不再存在或当前未使用的路径：

- `openmv`
- `graysensor`
- `PWMmotor`
- 旧的 `power` 路径

当前构建验证：

- 命令：`mingw32-make -j4`
- 结果：`Nothing to be done for 'all'.`
- 说明：当前生成物已经是最新状态，Makefile 可用。

## 当前已知注意项

1. 遥控器 DBUS 必须使用 `USART1_RX/PB7`，代码当前已是 `RemoteControlInit(&huart1)`。
2. 视觉通信当前使用 `USART6`，需要确认硬件接线和视觉端波特率。
3. `MPU6500` 方案只兼容原 INS 上层接口，不兼容原 BMI088/IST8310 底层驱动接口。
4. 如果实际不接 DJI 电机，需要从 `GimbalInit()` 开始处理电机初始化和反馈引用，否则仍会有离线日志。
5. `Core/Src/freertos.c` 中 `robotTaskHandle` 句柄赋值建议修正。
6. `StartINSTASK()` 的实际周期和注释不一致，后续应按需要统一。

